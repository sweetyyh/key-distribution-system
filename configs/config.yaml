app:
  name: key-distribution-system
  env: dev

server:
  host: 0.0.0.0
  port: 8080

mysql:
  host: 127.0.0.1
  port: 3306
  user: root
  password: change_me
  database: key_distribution_system
  max_idle_conns: 10
  max_open_conns: 50

redis:
  addr: 127.0.0.1:6379
  password: ""
  db: 0

jwt:
  buyer_secret: please_change_buyer_secret
  admin_secret: please_change_admin_secret
  expire_hours: 24

payment:
  channels:
    - name: epay
      enabled: true
      pid: your_pid
      key: your_key
      api_url: https://xxx.com/submit.php
      notify_url: https://yourdomain.com/api/v1/pay/callback/epay
      return_url: https://yourdomain.com/orders/{order_no}

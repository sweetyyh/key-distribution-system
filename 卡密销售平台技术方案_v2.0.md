# 卡密销售平台 — 技术方案

> **后端：Go (Gin + GORM + Redis)**
> **前端：Vue 3 + Element Plus**
> **版本：v2.0 | 2026年4月**

---

## 1. 项目定位

自营卡密销售平台。平台owner（即 admin）自己上架商品、导入卡密，用户注册后下单，第三方聚合支付收款，收款成功后自动返回卡密。发货失败则由 admin 手动退款转账。

**角色只有两个：**

| 角色 | 说明 |
|------|------|
| admin | 平台管理员，上架商品、导入卡密、处理订单、手动退款 |
| buyer | 注册用户，浏览商品、下单、查看卡密 |

---

## 2. 核心链路

```
用户注册/登录
    → 浏览商品 → 下单
    → 跳转第三方支付页面
    → 用户完成付款
    → 第三方回调通知 → 验签
    → 从 card_keys 取出卡密 → 写入 order_items
    → 订单状态更新为"已完成"
    → 用户在订单页永久查看卡密

异常路径：
    → 回调验签失败 → 记录日志，等待重试
    → 取卡密失败（库存不足）→ 订单标记"发货失败" → admin 手动退款转账
    → 超过 15 分钟未回调 → 定时任务主动查询支付状态
    → 30 分钟未支付 → 自动取消订单
```

---

## 3. 技术栈

| 层级 | 选型 |
|------|------|
| Web 框架 | Gin |
| ORM | GORM |
| 数据库 | MySQL |
| 缓存/锁 | Redis |
| 定时任务 | go-cron |
| 日志 | Zap |
| 配置 | Viper |
| 前端 | Vue 3 + Element Plus |

---

## 4. 数据库设计

### users 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| username | VARCHAR(64) | 用户名，唯一 |
| email | VARCHAR(128) | 邮箱，唯一 |
| password_hash | VARCHAR(256) | bcrypt 加密，cost ≥ 12 |
| role | ENUM | admin / buyer |
| status | TINYINT | 0=禁用 1=正常 |
| created_at | DATETIME | 注册时间 |

### categories 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| name | VARCHAR(64) | 分类名称 |
| parent_id | BIGINT | 父分类，顶级为 0 |
| sort_order | INT | 排序权重 |

### products 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| category_id | BIGINT FK | 分类 |
| name | VARCHAR(128) | 商品名称 |
| description | TEXT | 商品描述 |
| price | DECIMAL(18,2) | 单价 |
| wholesale_rules | JSON | 阶梯价，可为空 |
| stock | INT | 当前库存（冗余统计） |
| status | TINYINT | 0=下架 1=上架 |
| created_at | DATETIME | 创建时间 |

阶梯价格式：
```json
[
  { "min": 1,   "price": 10.00 },
  { "min": 100, "price": 9.00  }
]
```
为空则统一使用 `price` 字段。

### card_keys 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| product_id | BIGINT FK | 所属商品 |
| content | TEXT | 卡密内容（AES-256-GCM 加密） |
| status | TINYINT | 0=未售 1=锁定 2=已售 |
| order_id | BIGINT | 关联订单，未售时为 NULL |
| locked_at | DATETIME | 锁定时间 |
| sold_at | DATETIME | 售出时间 |

索引：`(product_id, status, id)` 联合索引，支持 FIFO 取卡。

### orders 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| order_no | VARCHAR(32) | 订单号，唯一，系统生成 |
| user_id | BIGINT FK | 买家 |
| product_id | BIGINT FK | 商品 |
| quantity | INT | 购买数量 |
| unit_price | DECIMAL(18,2) | 成交单价（下单时锁定） |
| total_amount | DECIMAL(18,2) | 订单总额 |
| status | TINYINT | 见下方状态机 |
| pay_channel | VARCHAR(32) | 支付渠道标识 |
| expires_at | DATETIME | 支付截止时间（下单后30分钟） |
| created_at | DATETIME | 下单时间 |
| paid_at | DATETIME | 支付时间 |

**订单状态机：**

```
[待支付 0] --30分钟超时--> [已取消 4]
[待支付 0] --支付成功--> [发货中 1] --发货成功--> [已完成 2]
                                     --发货失败--> [发货失败 3]
[发货失败 3] --admin手动退款后--> [已退款 5]
```

### payments 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| order_id | BIGINT FK | 关联订单 |
| order_no | VARCHAR(32) | 冗余订单号，便于对账 |
| out_trade_no | VARCHAR(64) | 我方请求单号，唯一 |
| trade_no | VARCHAR(64) | 第三方交易号 |
| channel | VARCHAR(32) | 支付渠道（alipay/wxpay 等） |
| amount | DECIMAL(18,2) | 支付金额 |
| status | TINYINT | 0=待支付 1=成功 2=失败 3=已关闭 |
| raw_callback | JSON | 原始回调报文，保留用于对账 |
| paid_at | DATETIME | 支付完成时间 |
| created_at | DATETIME | 创建时间 |

唯一索引：`out_trade_no`、`(channel, trade_no)`（回调幂等）。

### order_items 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| order_id | BIGINT FK | 关联订单 |
| card_key_id | BIGINT FK | 关联卡密 |
| content_snapshot | TEXT | 发货时卡密明文快照（加密存储） |
| delivered_at | DATETIME | 发货时间 |

说明：用户查看卡密时从 `content_snapshot` 解密返回，即使原 `card_keys` 记录被修改也不影响。

### product_snapshots 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| order_id | BIGINT FK | 关联订单 |
| product_id | BIGINT | 商品 ID（冗余） |
| name | VARCHAR(128) | 下单时商品名 |
| price | DECIMAL(18,2) | 下单时单价 |
| wholesale_rules | JSON | 下单时阶梯价 |
| created_at | DATETIME | 快照时间 |

防止商品价格或名称修改后历史订单展示错乱。

---

## 5. 核心业务流程

### 5.1 下单流程

```
1. 校验商品状态、库存 ≥ 购买数量
2. 计算成交单价（匹配阶梯价）
3. 写入 orders（状态：待支付）、product_snapshots
4. Redis DECRBY 预扣库存
   → 扣减后 < 0 → 回滚 → 返回"库存不足"
5. DB 事务：SELECT ... FOR UPDATE SKIP LOCKED 锁定 N 张卡密
   → 更新 card_keys.status = 1（锁定），写入 order_id、locked_at
   → 锁卡失败 → 回滚 Redis → 取消订单 → 返回"库存不足"
6. 写入 payments 记录（状态：待支付）
7. 调用第三方支付 API 生成支付链接/二维码
8. 返回支付链接给前端
```

### 5.2 支付回调处理

```
1. 验证第三方签名 → 失败则返回 fail，记录告警日志
2. Redis 加锁：pay_callback:{channel}:{trade_no}，TTL 60s
   → 加锁失败说明正在处理，直接返回 success
3. 查询 payments / orders 状态
   → 非"待支付" → 幂等返回 success
4. 开启 DB 事务：
   a. 更新 payments.status = 1，写入 trade_no、paid_at、raw_callback
   b. 更新 orders.status = 1（发货中），写入 paid_at
   c. 批量更新 card_keys.status = 2（已售），写入 sold_at
   d. 写入 order_items（含 content_snapshot）
   e. 更新 orders.status = 2（已完成）
   f. 更新 products.stock -= quantity
   → 全部成功：提交事务，返回 success
   → 任意失败：回滚事务，订单标记"发货失败"，告警通知 admin
5. 释放 Redis 锁
```

注意：第三方支付成功是外部事实，不能因为本地事务失败而要求第三方撤销。发货失败只能走退款补偿，不能回滚支付。

### 5.3 超时处理

定时任务每 2 分钟扫描：

```
待支付订单 AND expires_at < NOW()
    → 更新 orders.status = 4（已取消）
    → 批量更新关联 card_keys.status = 0（未售），清空 order_id
    → Redis INCRBY 回补库存
    → 调用第三方关单 API（如支持）
```

### 5.4 回调未到达补偿

定时任务每 5 分钟扫描：

```
payments.status = 0（待支付）AND created_at < NOW() - 5min
    → 主动调用第三方查单 API
    → 若已支付 → 触发 5.2 流程
    → 若未支付且订单已超时 → 执行 5.3 流程
```

### 5.5 发货失败处理

发货失败的订单（status=3）：

- 前端展示"发货失败，请联系客服"
- admin 后台可查看所有发货失败订单
- admin 核实后手动转账退款
- admin 在后台将订单标记为"已退款"（status=5），填写退款备注
- 系统自动将关联的冻结卡密回收（status=0）或作废

---

## 6. 第三方支付接入

### 6.1 选型说明

待选，建议从以下几类里确认：

| 类型 | 代表服务 | 特点 |
|------|---------|------|
| 聚合收款（无需资质） | 易支付(Epay)、虎皮椒、PayJS | 个人可用，支持支付宝/微信，接入快 |
| 官方渠道 | 支付宝当面付、微信商户号 | 需企业资质，稳定性高 |
| 加密货币 | OxaPay、Plisio | 适合海外/匿名用户，收 USDT/BTC |

选定后需补充：回调字段定义、签名算法、退款 API 是否支持。

### 6.2 接入要点（通用）

无论选哪家，以下几点必须实现：

- `out_trade_no` 全局唯一，建议格式：`{yyyyMMddHHmmss}{orderNo}`
- 回调签名必须服务端校验，不能信任前端传参
- 回调接口必须幂等（见 5.2）
- 金额单位确认（分 vs 元），避免多收少收
- 生产环境必须 HTTPS，回调地址不能是 IP

### 6.3 支付渠道配置表

在 `configs/` 中维护，不硬编码：

```yaml
payment:
  channels:
    - name: epay
      enabled: true
      pid: "your_pid"
      key: "your_key"
      api_url: "https://xxx.com/submit.php"
      notify_url: "https://yourdomain.com/api/v1/pay/callback/epay"
      return_url: "https://yourdomain.com/orders/{order_no}"
```

---

## 7. 并发一致性

高并发下单时防止超卖和一卡多售：

**第一层：Redis 预扣**

```
DECRBY product:{id}:stock {quantity}
→ 结果 < 0 → INCRBY 回滚 → 返回库存不足
```

**第二层：DB 悲观锁**

```sql
BEGIN;
SELECT id FROM card_keys
WHERE product_id = ? AND status = 0
ORDER BY id ASC LIMIT ?
FOR UPDATE SKIP LOCKED;

UPDATE card_keys SET status = 1, order_id = ?, locked_at = NOW()
WHERE id IN (...);
COMMIT;
```

`SKIP LOCKED` 保证并发请求不互相阻塞，各取各的卡密。

**第三层：定时对账**

每 10 分钟校验 `products.stock` 是否等于 `COUNT(card_keys WHERE status=0 AND product_id=?)`，不一致以 card_keys 为准修正，同步更新 Redis。

---

## 8. 安全设计

- 卡密内容 AES-256-GCM 加密存储和展示，密钥通过环境变量注入，不入代码库
- 用户密码 bcrypt 存储，cost ≥ 12
- 卡密查看接口校验：必须登录 + 必须是本人订单 + 订单状态为已完成
- order_id 不使用自增整数对外暴露，改用 order_no（雪花ID或UUID）防枚举
- 支付回调接口仅允许第三方 IP 段访问（如支持白名单则配置）
- admin 后台路由单独前缀 `/admin`，独立 JWT 签发，与买家 token 隔离
- 所有金额字段使用 `DECIMAL(18,2)`，禁止浮点数参与金额运算
- SQL 参数绑定，禁止拼接 SQL

---

## 9. API 接口

### 买家端

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/auth/register | 注册 |
| POST | /api/v1/auth/login | 登录 |
| GET | /api/v1/products | 商品列表 |
| GET | /api/v1/products/:id | 商品详情 |
| POST | /api/v1/orders | 创建订单，返回支付链接 |
| GET | /api/v1/orders/:order_no | 订单详情 |
| GET | /api/v1/orders/:order_no/cards | 查看卡密（永久） |
| GET | /api/v1/orders | 我的订单列表 |

### 支付回调

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/pay/callback/:channel | 第三方异步通知 |
| GET | /api/v1/pay/return/:channel | 第三方同步跳转（仅展示，不作为支付凭据） |

### Admin 后台

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/admin/products | 上架商品 |
| PUT | /api/admin/products/:id | 编辑商品 |
| POST | /api/admin/products/:id/cards | 批量导入卡密 |
| GET | /api/admin/orders | 订单列表（含筛选） |
| PUT | /api/admin/orders/:order_no/refund | 标记已退款 |
| GET | /api/admin/orders/failed | 发货失败订单 |
| GET | /api/admin/stats | 销售统计 |

---

## 10. 项目目录结构

```
├── cmd/
│   └── server/main.go
├── internal/
│   ├── config/
│   ├── model/                  # GORM 数据模型
│   ├── repository/             # 数据访问层
│   ├── service/
│   │   ├── user_service.go
│   │   ├── product_service.go
│   │   ├── order_service.go    # 下单、锁卡、发卡
│   │   ├── payment_service.go  # 回调处理、主动查单
│   │   └── card_service.go     # 卡密导入、查看
│   ├── handler/                # HTTP 处理器
│   ├── middleware/             # JWT、限流、admin鉴权
│   ├── router/
│   ├── cron/
│   │   ├── order_timeout.go    # 超时取消
│   │   ├── payment_query.go    # 主动查单补偿
│   │   └── stock_sync.go       # 库存对账
│   └── pkg/
│       ├── crypto/             # AES 加解密
│       ├── payment/            # 支付渠道适配器接口
│       │   ├── interface.go    # 统一接口定义
│       │   └── epay/           # 具体渠道实现（待补充）
│       ├── lock/               # Redis 分布式锁
│       └── response/           # 统一响应格式
├── migrations/
├── deploy/
│   ├── Dockerfile
│   └── docker-compose.yml
├── configs/
│   ├── config.yaml
│   └── config.prod.yaml
├── go.mod
└── go.sum
```

---

## 11. 部署

```yaml
# docker-compose.yml 服务清单
services:
  app:       # Go 服务
  mysql:     # 主数据库
  redis:     # 缓存与分布式锁
  nginx:     # 反向代理 + SSL
```

生产环境必做：

- MySQL 开启定时备份（每日全量 + binlog）
- Redis 开启 AOF 持久化
- Nginx 配置 HTTPS，支付回调地址必须 HTTPS
- 环境变量管理 AES 密钥、JWT 密钥、支付渠道密钥，不入代码库

---

## 12. 开发排期

| 阶段 | 周期 | 内容 |
|------|------|------|
| P0 | 1 周 | 用户注册登录、商品管理、卡密导入、下单锁卡 |
| P1 | 1 周 | 第三方支付接入、回调处理、自动发卡、订单查看卡密 |
| P2 | 3 天 | 超时取消、补偿查单、发货失败处理、admin退款标记 |
| P3 | 3 天 | admin 后台、销售统计、安全加固 |
| P4 | 持续 | 支付渠道切换、性能优化 |

**P0+P1 跑通即可上线收款，P2+P3 补完运营能力。**
